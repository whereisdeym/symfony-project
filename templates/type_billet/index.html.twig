{% extends 'base.html.twig' %}

{% block title %}TypeBillet index{% endblock %}

{% block body %}
    <h1>TypeBillet index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Classe</th>
                <th>Services</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_billet in type_billets %}
            <tr>
                <td>{{ type_billet.id }}</td>
                <td>{{ type_billet.classe }}</td>
                <td>{{ type_billet.Services }}</td>
                <td>
                    <a href="{{ path('app_type_billet_show', {'id': type_billet.id}) }}">show</a>
                    <a href="{{ path('app_type_billet_edit', {'id': type_billet.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_billet_new') }}">Create new</a>
{% endblock %}
